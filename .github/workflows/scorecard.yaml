name: Scorecard
on:
  workflow_dispatch:
  push:
    branches:
      - main
  schedule:
    - cron:  '30 1 * * 6'

permissions: read-all

jobs:
  workflow:
    name: Reusable Workflow
    uses: action-stars/generic-workflows/.github/workflows/scorecard.yaml@9ebbb6e9360565432fd1898fda091048be02db1b # v0.12.0
    permissions:
      id-token: write
      security-events: write
    with:
      retention_days: 7
